# AI-SEARCH-VISUALUSER
This project demonstrates how search algorithms in Artificial Intelligence are used to find paths between nodes in a graph. It implements both uninformed algorithms (BFS, DFS) and informed algorithms (A*) to compare their efficiency and behavior. 
